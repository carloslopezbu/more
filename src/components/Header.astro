---
import "@/styles/global.css";

const sections: { id: string; name: string }[] = [
    { id: "#home", name: "iniciar sesión" },
];
---

<header class="flex items-center justify-end p-4">
    <nav>
        <a
            href="#home"
            class="bg-violet-400 text-white font-semibold rounded-full shadow-lg px-5 py-2 transform transition-all duration-300 ease-in-out hover:bg-violet-500 hover:scale-105"
        >
            Iniciar sesión
        </a>
    </nav>
</header>
